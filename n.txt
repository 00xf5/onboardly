Onboardly — Full Features & Flow
1️⃣ Core Objective

Automate client onboarding for freelancers & agencies:

Collect client info

Send emails automatically

Assign tasks/checklists

Track progress in a simple dashboard

Monetize via subscriptions ($10/mo)

2️⃣ User Roles
Role	Access / Actions
Admin (freelancer/agency)	Create/edit templates, manage tasks & emails, view dashboard, handle clients, subscription management, see analytics
Client	Fill onboarding forms, view tasks, upload files
3️⃣ Core Features
A. Onboarding Template Builder

Admin creates templates:

Task checklist: e.g., “Sign contract”, “Send assets”, “Confirm payment”

Email templates with placeholders {client_name}, {project_name}

Optional deadlines per task

B. Client Form

Auto-generated per template

Collects:

Name, email, phone, company

Project info

File uploads (optional)

Generates a unique client onboarding session

C. Email Automation

Welcome email triggered on form submission

Task reminders sent via scheduled function

SMTP / SendGrid / Mailgun integration

D. Task Dashboard

Per-client task checklist:

Completed / pending tasks

Progress bar (visual)

Admin dashboard shows all clients and progress

Optional: export CSV / PDF

E. Analytics & Reporting

Active clients

Task completion rates

Emails sent & opened (optional)

F. Subscription & Monetization

Free: 1 template, 1 client

Pro ($10/mo): unlimited templates & clients

Optional add-ons: extra email credits, export, white-label

4️⃣ Serverless Architecture Flow
[Admin creates template] ---> stored in DB
         |
[Client opens unique form link] 
         |
[submitClientForm Function triggers]
         |--> store client info in DB
         |--> assign tasks
         |--> trigger welcome email
         v
[Dashboard API Function] ---> Admin sees tasks & progress
         |
[Cron / Scheduled Function]
         |--> sends task reminder emails for incomplete tasks
         v
[Stripe Webhook Function]
         |--> updates subscription status (free / pro)

5️⃣ Database Schema (MongoDB example)

Clients Collection

{
  "_id": "client_id",
  "name": "John Doe",
  "email": "john@example.com",
  "template_id": "template123",
  "tasks": [
    { "name": "Sign Contract", "completed": false, "deadline": "2026-02-01" },
    { "name": "Provide Assets", "completed": false, "deadline": "2026-02-03" }
  ],
  "submission_date": "2026-01-29T14:00:00Z"
}


Templates Collection

{
  "_id": "template123",
  "name": "Standard Onboarding",
  "tasks": ["Sign Contract", "Provide Assets", "Confirm Payment"],
  "emails": [
    { "subject": "Welcome, {client_name}", "body": "Hello {client_name}, welcome to the project {project_name}" }
  ],
  "created_by": "admin_id"
}


Admin Collection

{
  "_id": "admin_id",
  "name": "Jane Freelancer",
  "email": "jane@example.com",
  "plan": "free|pro",
  "templates": ["template123"],
  "clients": ["client_id1", "client_id2"]
}

6️⃣ Serverless Functions (Core MVP)
Function Name	Trigger	Description
submitClientForm	HTTP POST	Stores client info, assigns tasks, triggers welcome email
sendWelcomeEmail	Internal	Sends templated welcome email
getDashboardData	HTTP GET	Returns client task progress for admin
sendTaskReminders	Scheduled / Cron	Sends emails for incomplete tasks
stripeWebhookHandler	Stripe Webhook	Updates subscription plan, upgrades/downgrades
manageTemplates	HTTP CRUD	Create/edit/delete templates
analyticsFetcher	HTTP GET	Returns optional stats for dashboard
7️⃣ Onboardly Flow Step-by-Step

Admin creates template → tasks + emails → stored in DB

Client receives form link → fills out info → submitClientForm function

Serverless function triggers:

Save client in DB

Assign tasks

Send welcome email

Dashboard updated → Admin sees tasks + progress

Scheduled reminders → sends email reminders for incomplete tasks

Stripe handles subscription → free / pro features unlocked automatically

8️⃣ MVP Launch Plan

1 template, 1 form, dashboard, welcome email

Stripe subscription integration (free + pro $10/mo)

Host frontend on Vercel, backend as serverless functions

DB: MongoDB Atlas / Supabase

Optional cron jobs for reminders via Vercel Cron or AWS EventBridge

💡 Pro Tips for Success

Keep placeholders in emails for personalization → instant “wow” effect

Make the dashboard visually simple → progress bars, checkmarks

SEO landing page: “Client onboarding automation for freelancers” → organic traffic

Micro-analytics → email open rate, task completion → shows value