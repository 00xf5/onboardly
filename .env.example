# === Firebase (server-side: use Vercel Environment Variables) ===
# You can either paste the raw multiline PRIVATE_KEY into FIREBASE_PRIVATE_KEY
# or set FIREBASE_PRIVATE_KEY_BASE64 to the base64-encoded value (recommended).
FIREBASE_PROJECT_ID=onboardly-c3c9e
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@onboardly-c3c9e.iam.gserviceaccount.com
# Option A: raw multiline (may need to escape newlines when adding to Vercel)
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# Option B (recommended): base64 encoded private key
FIREBASE_PRIVATE_KEY_BASE64=PASTE_BASE64_ENCODED_PRIVATE_KEY_HERE
FIREBASE_STORAGE_BUCKET=onboardly-c3c9e.appspot.com

# === NowPayments ===
NOWPAYMENTS_API_KEY=PASTE_NOWPAYMENTS_API_KEY_HERE
NOWPAYMENTS_WEBHOOK_SECRET=PASTE_NOWPAYMENTS_WEBHOOK_SECRET_HERE
NOWPAYMENTS_WIDGET_IID=6136362268

# === Email (SendGrid or SMTP) ===
SENDGRID_API_KEY=PASTE_SENDGRID_API_KEY_HERE
EMAIL_FROM=hello@yourdomain.com

# === App / Pricing (editable) ===
PRICE_USD_PREMIUM=10
CURRENCY=USD

# === Optional / Cross-instance services (recommended for cache & cron) ===
REDIS_URL=redis://:password@host:6379
SENTRY_DSN=

# === Public (client-side) Firebase config for Vite (prefix with VITE_) ===
# These are safe to expose to the browser and are used by the frontend Firebase SDK.
VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN=onboardly-c3c9e.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=onboardly-c3c9e
VITE_FIREBASE_STORAGE_BUCKET=onboardly-c3c9e.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID=YOUR_FIREBASE_APP_ID

# === Node env ===
NODE_ENV=production

# === Notes ===
# 1) To create a base64 value for FIREBASE_PRIVATE_KEY_BASE64 on macOS / Linux:
#    printf '%s' "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----" | base64
# 2) In your serverless function, decode as:
#    const privateKey = Buffer.from(process.env.FIREBASE_PRIVATE_KEY_BASE64, 'base64').toString('utf8');
# 3) Add these variables to Vercel's project > Settings > Environment Variables (Production scope).
# 4) Rotate keys if you accidentally paste secrets in chat or commit them to the repo.
